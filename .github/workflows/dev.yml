name: CI/CD Prod

on:
  push:
    branches:
      - dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Docker Login
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker Image
        run: docker build -t kevinirawand/sia-banjar-dev:latest .

      - name: Push Docker Image
        run: docker push kevinirawand/sia-banjar-dev:latest

      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          script: |
            docker pull kevinirawand/sia-banjar-dev:latest
            docker stop sia-banjar-dev || true
            docker rm sia-banjar-dev || true
            docker run -d --name sia-banjar-dev \
              -e MONGO_URI=${{ secrets.DEV_MONGO_URI }} \
              -e PORT=${{ secrets.DEV_PORT }} \
              -p ${{secrets.DEV_PORT}}:${{secrets.DEV_PORT}} \
              kevinirawand/sia-banjar-dev:latest